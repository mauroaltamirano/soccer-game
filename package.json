{
  "name": "soccer-game",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter web dev",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  }
}
